# Inference worker dependencies
# Install with:  pip install -r workers/inference/requirements.txt
#
# GPU (CUDA 12.1) — replace with the wheel matching your driver:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU-only (default):
torch>=2.1.0
torchvision>=0.16.0
# CLIP backbone + processor
transformers>=4.38.0
# Video frame extraction
opencv-python-headless>=4.8.0
# Image processing
Pillow>=10.0.0
# Numerical
numpy>=1.24.0
# Artifact rendering (timeline PNG, thumbnail strip)
matplotlib>=3.8.0
